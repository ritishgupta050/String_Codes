#include<bits/stdc++>
using namespace std;

int main()
{
    int n;
    cin>>n;
    string s;

    while(n--)
    {
        cin>>s;

        set<int> count;

        for(int i=0;i<s.size();i++)
        {
            count.insert(s[i]);

        }
        cout<<count.size()<<"\n";
    }
return 0;
}
